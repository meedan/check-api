<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p><%= I18n.t(:mail_view_welcome, default: "Welcome to %{product_name}", product_name: CONFIG['product_name']) %>, <%= @resource.name %>!</p>
    <p><%= I18n.t(:mail_view_register, default: "You are only one step away from using %{product_name}! Please confirm your email address by clicking the following link:", product_name: CONFIG['product_name']) %></p>
    <p><%= confirmation_url(@resource, host: @host, confirmation_token: @resource.confirmation_token, client_host: @client_host) %></p>
  </body>
</html>
