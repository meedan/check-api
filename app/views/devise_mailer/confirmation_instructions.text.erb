<%= I18n.t(:mail_view_welcome, default: "Welcome to %{product_name}", product_name: CONFIG['product_name']) %>, <%= @resource.name %>!

<%= I18n.t(:mail_view_register, default: "You are only one step away from using %{product_name}! Please confirm your email address by clicking the following link:", product_name: CONFIG['product_name']) %>

<%= confirmation_url(@resource, host: @host, confirmation_token: @resource.confirmation_token, client_host: @client_host) %>
