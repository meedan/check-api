Welcome to Check, <%= @resource.name %>!

Go to <%= confirmation_url(@resource, host: @host, confirmation_token: @resource.confirmation_token, client_host: @client_host) %> to confirm your e-mail address.

If you didn't want to join Check, just ignore this message.
