<%= I18n.t(:"devise.mailer.invitation_instructions.hello", name: @resource.name) %>

<p><%= I18n.t(:"devise.mailer.invitation_instructions.someone_invited_you_#{@invited_type}", name: @invited_by, team: @url) %></p>


<% if @invited_text %>
	 <%= @invited_text %>
<% end %>

<%= I18n.t(:"devise.mailer.invitation_instructions.accept") %>

<% if @resource.invitation_due_at %>
  <%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
<% end %>

<%= t("devise.mailer.invitation_instructions.ignore") %>

<%= I18n.t(:"devise.mailer.invitation_instructions.app_team", app: CONFIG['app_name']) %>