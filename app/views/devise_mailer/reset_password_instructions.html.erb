<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p><%= I18n.t('devise.mailer.reset_password_instructions.greeting', :recipient => @resource.email) %></p>

    <p><%= I18n.t('devise.mailer.reset_password_instructions.instruction') %></p>

    <p><%= link_to I18n.t('devise.mailer.reset_password_instructions.action'), edit_password_url(@resource, host: @host, reset_password_token: @token, format: :json) %></p>

    <p><%= t('devise.mailer.reset_password_instructions.instruction_2') %></p>
    <p><%= t('devise.mailer.reset_password_instructions.instruction_3') %></p>
  </body>
</html>
