namespace :check do
  namespace :migrate do
    task remove_project_source: :environment do
    	# TODO
    	# Remove project source
    	# Remove project source annotations
    	# Remove verions related to project source annotation
    	# Remove Cache - project_source_id_cache_for_project_media_*
    end
  end
end