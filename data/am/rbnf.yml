am: 
  rbnf: 
    grouping: 
      - 
        ? :ruleset
        : 
          - 
            ? :rules
            : 
              - 
                ? :rule
                : "ቅንስናሽ >>;"
                ? :value
                : "-x"
              - 
                ? :rule
                : "=#,###0.#=;"
                ? :value
                : x.x
              - 
                ? :rule
                : =%spellout-numbering=;
                ? :value
                : 0
              - 
                ? :radix
                : 100
                ? :rule
                : "<< መቶ[ >>];"
                ? :value
                : 1100
              - 
                ? :rule
                : =%spellout-numbering=;
                ? :value
                : 10000
            ? :type
            : spellout-numbering-year
          - 
            ? :rules
            : 
              - 
                ? :rule
                : =%spellout-cardinal=;
                ? :value
                : 0
            ? :type
            : spellout-numbering
          - 
            ? :rules
            : 
              - 
                ? :rule
                : "ቅንስናሽ >>;"
                ? :value
                : "-x"
              - 
                ? :rule
                : "<< ነጥብ >>;"
                ? :value
                : x.x
              - 
                ? :rule
                : ባዶ;
                ? :value
                : 0
              - 
                ? :rule
                : አንድ;
                ? :value
                : 1
              - 
                ? :rule
                : ሁለት;
                ? :value
                : 2
              - 
                ? :rule
                : ሦስት;
                ? :value
                : 3
              - 
                ? :rule
                : አራት;
                ? :value
                : 4
              - 
                ? :rule
                : አምስት;
                ? :value
                : 5
              - 
                ? :rule
                : ስድስት;
                ? :value
                : 6
              - 
                ? :rule
                : ሰባት;
                ? :value
                : 7
              - 
                ? :rule
                : ስምንት;
                ? :value
                : 8
              - 
                ? :rule
                : ዘጠኝ;
                ? :value
                : 9
              - 
                ? :rule
                : "አስር[ >>];"
                ? :value
                : 10
              - 
                ? :rule
                : "<< አስር[ >>];"
                ? :value
                : 20
              - 
                ? :rule
                : "መቶ[ >>];"
                ? :value
                : 100
              - 
                ? :rule
                : "<< መቶ[ >>];"
                ? :value
                : 200
              - 
                ? :rule
                : "ሺ[ >>];"
                ? :value
                : 1000
              - 
                ? :rule
                : "<< ሺ[ >>];"
                ? :value
                : 2000
              - 
                ? :rule
                : "ሚሊዮን[ >>];"
                ? :value
                : 1000000
              - 
                ? :rule
                : "<< ሚሊዮን[ >>];"
                ? :value
                : 2000000
              - 
                ? :rule
                : "<< ቢሊዮን[ >>];"
                ? :value
                : 1000000000
              - 
                ? :rule
                : "<< ቲሪሊዮን[ >>];"
                ? :value
                : 1000000000000
              - 
                ? :rule
                : "<< ቈዲሪሊዮን[ >>];"
                ? :value
                : 1000000000000000
              - 
                ? :rule
                : "=#,##0=;"
                ? :value
                : 1000000000000000000
            ? :type
            : spellout-cardinal
          - 
            ? :rules
            : 
              - 
                ? :rule
                : "ቅንስናሽ >>;"
                ? :value
                : "-x"
              - 
                ? :rule
                : "=#,##0.#=;"
                ? :value
                : x.x
              - 
                ? :rule
                : =%spellout-numbering=ኛ;
                ? :value
                : 0
            ? :type
            : spellout-ordinal
        ? :type
        : SpelloutRules
      - 
        ? :ruleset
        : 
          - 
            ? :rules
            : 
              - 
                ? :rule
                : −>>;
                ? :value
                : "-x"
              - 
                ? :rule
                : "=#,##0=ኛ;"
                ? :value
                : 0
            ? :type
            : digits-ordinal
        ? :type
        : OrdinalRules